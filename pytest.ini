[pytest]
markers =
    gpu: tests that require a CUDA GPU and are opt-in via MICROJAX_GPU_TESTS=1
    slow: tests that trade speed for cross-library consistency checks
testpaths =
    tests/unittests
norecursedirs =
    tests/integrate
